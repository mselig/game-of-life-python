language: python
python:
    - "3.6"

sudo: false

install:
    - pip install pytest
    - pip install pytest-cov
    - pip install coveralls

script:
    - pytest --cov=gol/
    - pytest --pyargs gol
    - pytest gol.test.test

after_success:
    - coveralls

